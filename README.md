# Портфолио qwertyiisme

Современный одностраничный сайт-портфолио с продуманным дизайном, плавными анимациями и полной адаптивностью.

## ✨ Особенности

- 🎨 **Современный дизайн** - Чистый и минималистичный интерфейс с градиентами и плавными переходами
- 🌓 **Темная/Светлая тема** - Переключение между темами с сохранением выбора в localStorage
- 📱 **Полная адаптивность** - Идеально работает на всех устройствах (desktop, tablet, mobile)
- ⚡ **Производительность** - Оптимизированный код с lazy loading, Intersection Observer, debounce и GPU-ускорением
- 🎭 **Плавные анимации** - Анимации при скролле, typing эффект, параллакс
- 🎯 **Интерактивность** - Интерактивные элементы, hover эффекты, кастомный курсор
- 🎵 **Strudel фоновый плеер** - Встроенная расслабляющая ambient музыка на базе Strudel, играет автоматически на заднем фоне
- ♿ **Доступность** - Семантическая разметка, ARIA атрибуты, keyboard navigation
- 🔍 **SEO оптимизация** - Мета-теги, Open Graph, оптимизированная структура для поисковых систем

## 🚀 Технологии

- **HTML5** - Семантическая разметка
- **CSS3** - Современные возможности (CSS Variables, Grid, Flexbox, Animations)
- **JavaScript (ES6+)** - Vanilla JS без зависимостей
- **Google Fonts** - Inter и JetBrains Mono
- **Strudel** - Платформа для алгоритмической музыки и live coding ([strudel.cc](https://strudel.cc/))

## 📁 Структура проекта

```
qwertyiisme.github.io/
├── index.html      # Основной HTML файл
├── styles.css      # Стили и анимации
├── script.js       # JavaScript функциональность
├── .nojekyll       # Отключение Jekyll для GitHub Pages
├── avatarca.jpg    # Аватар
└── README.md       # Документация
```

## 🎯 Секции сайта

1. **Hero Section** - Главная секция с анимированным терминалом
2. **О себе** - Информация о разработчике и используемых технологиях
3. **Проекты** - Карточки с избранными проектами
4. **Навыки** - Интерактивные прогресс-бары навыков
5. **Контакты** - Форма обратной связи и ссылки на соцсети

## 🛠️ Функциональность

### Навигация
- Фиксированная навигация с прозрачным фоном
- Плавная прокрутка к секциям
- Автоматическое выделение активной секции
- Мобильное меню-бургер

### Темная тема
- Переключение темы кнопкой в навигации
- Сохранение выбора в localStorage
- Плавный переход между темами
- Горячая клавиша: `Ctrl/Cmd + K`

### Анимации
- Typing эффект в терминале
- Fade-in анимации при скролле
- Параллакс эффект для фоновых элементов
- Hover эффекты на интерактивных элементах
- Анимированные прогресс-бары навыков

### Форма обратной связи
- Валидация полей
- Анимация отправки
- Симуляция API запроса

### Strudel фоновый плеер 🎵
- **Автоматическое воспроизведение** расслабляющей ambient музыки на заднем фоне
- Встроенная интеграция с [Strudel.cc](https://strudel.cc/) - платформой для алгоритмической музыки
- Многослойная композиция с медленными аккордами, фильтрами и реверберацией
- Музыка играет автоматически при загрузке страницы
- Невидимый iframe - музыка не мешает просмотру сайта

## 🎨 Кастомизация

### Цветовая схема

Цвета настраиваются через CSS переменные в `styles.css`:

```css
:root {
    --accent-primary: #667eea;
    --accent-secondary: #764ba2;
    /* ... другие переменные */
}
```

### Шрифты

Используются Google Fonts:
- **Inter** - Основной шрифт
- **JetBrains Mono** - Моноширинный для кода

### Контент

Все текстовые данные находятся в `index.html`. Просто отредактируйте соответствующие секции.

### Настройка фоновой музыки Strudel

Фоновая музыка создается с помощью Strudel - платформы для алгоритмической музыки. Чтобы изменить композицию:

1. Откройте `index.html` и найдите секцию `strudel-background-player`
2. Измените код в атрибуте `src` iframe (после `#`)
3. Используйте синтаксис Strudel для создания своей композиции

**Текущая композиция (с эффектами):**
```javascript
stack(
  // Основной слой: аккорды с модулирующим фильтром и панорамированием
  note("<c3 eb3 g3>*4 <bb2 f3 d4>*4 <a2 f3 c4>*4 <bb2 g3 eb4>*4")
    .s("sine")
    .gain(0.12)
    .slow(8)
    .lpf(sine.range(300, 1500).slow(12))  // Модуляция фильтра
    .adsr(".2:.1:.6:.4")  // Плавная огибающая
    .pan(sine.range(-0.3, 0.3).slow(16)),  // Стерео панорамирование
  
  // Средний слой: высокие ноты с delay и reverb
  note("<c4 eb4 g4>*2 <bb3 f4 d5>*2")
    .s("triangle")
    .gain(0.08)
    .slow(6)
    .delay(0.4).delayfeedback(0.25)  // Эхо эффект
    .lpf(saw.range(500, 2000).slow(8))  // Пилообразная модуляция
    .adsr(".3:.2:.5:.5")
    .room(0.4),  // Реверберация
  
  // Бас слой: глубокий низкочастотный фон
  note("c2 bb1 f2 eb2")
    .s("sawtooth")
    .gain(0.06)
    .slow(12)
    .lpf(600)
    .adsr(".4:.2:.4:.6")
    .room(0.5)
    .pan(tri.range(-0.2, 0.2).slow(20))  // Медленное панорамирование
).slow(2)
```

**Используемые эффекты:**
- **ADSR** - плавные огибающие для естественного звука
- **lpf с сигналами** - динамические фильтры (sine, saw, tri)
- **delay** - эхо эффекты для глубины
- **room** - реверберация для пространства
- **pan** - стерео панорамирование
- **range** - модуляция параметров в заданных диапазонах

**Примеры кода Strudel:**
```javascript
// Простая мелодия
note("c3 eb3 g3")
  .slow(2)
  .s("sawtooth")
  .gain(0.3)

// С эффектами
"c3 eb3 g3"
  .s("sawtooth")
  .lpf(sine.range(200, 8000).slow(4))
  .gain(0.3)
```

Подробнее о синтаксисе Strudel: [strudel.cc](https://strudel.cc/)

**Примечание:** Музыка воспроизводится автоматически при загрузке страницы. Некоторые браузеры могут требовать взаимодействия пользователя для автовоспроизведения аудио.

## 📱 Адаптивность

Сайт полностью адаптивен и оптимизирован для:
- Desktop (1200px+)
- Tablet (768px - 1199px)
- Mobile (до 767px)

## 🚀 Развертывание

### GitHub Pages

1. Загрузите файлы в репозиторий
2. Включите GitHub Pages в настройках репозитория
3. Выберите ветку `main` и папку `/root`
4. Сайт будет доступен по адресу: `https://qwertyiisme.github.io`

### Локальный запуск

Просто откройте `index.html` в браузере или используйте локальный сервер:

```bash
# Python
python -m http.server 8000

# Node.js (http-server)
npx http-server

# PHP
php -S localhost:8000
```

## 🔧 Оптимизация

### Производительность
- ✅ Preload критических ресурсов (CSS, JS, шрифты)
- ✅ Debounce для обработчиков событий скролла
- ✅ GPU-ускорение анимаций (transform, will-change)
- ✅ Lazy loading для изображений
- ✅ Intersection Observer для анимаций
- ✅ CSS Variables для быстрого переключения тем
- ✅ Оптимизация шрифтов (async загрузка)
- ✅ Минимизация repaints и reflows

### SEO и метаданные
- ✅ Open Graph теги для социальных сетей
- ✅ Twitter Card метаданные
- ✅ Оптимизированные meta description и keywords
- ✅ Семантическая HTML разметка
- ✅ Favicon (SVG)

### GitHub Pages
- ✅ Файл `.nojekyll` для отключения Jekyll
- ✅ Оптимизированная структура для статического хостинга
- ✅ Правильные пути к ресурсам

## 📝 Лицензия

Этот проект создан для демонстрации навыков и может быть использован как шаблон для личного портфолио.

## 👨‍💻 Автор

**qwertyiisme**

- GitHub: [@qwertyiisme](https://github.com/qwertyiisme)
- Сайт: [qwertyiisme.github.io](https://qwertyiisme.github.io)

---

Создано с ❤️ и кофе ☕

